generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
<<<<<<< HEAD
  id                        String         @id
  email                     String         @unique
  name                      String?
  password                  String?
  twoFactorSecret           String?
  twoFactorEnabled          Boolean        @default(false)
  emailVerified             Boolean        @default(false)
  verificationToken         String?
  createdAt                 DateTime       @default(now())
  updatedAt                 DateTime       @updatedAt
  resetPasswordToken        String?
  resetPasswordTokenExpiry  DateTime?
  geoMapPermissions         Json?          @default("{\"read\": true, \"edit\": false, \"admin\": false}")
  
  loginHistory              LoginHistory[]
  apiKeys                   ApiKey[]
}

model LoginHistory {
  id          String   @id @default(uuid())
  userId      String
  action      String   // 'login', 'logout', 'geomap_access'
  device      String?
  country     String?
  timestamp   DateTime @default(now())
  
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model ApiKey {
  id          String    @id @default(uuid())
  userId      String
  keyHash     String
  name        String
  scopes      String[]  @default(["read"])
  expiresAt   DateTime?
  lastUsed    DateTime?
  createdAt   DateTime  @default(now())
  
  user        User      @relation(fields: [userId], references: [id], onDelete: Cascade)
}
=======
  id                    String         @id
  email                 String         @unique
  name                  String?
  password              String?
  twoFactorSecret       String?
  twoFactorEnabled      Boolean        @default(false)
  emailVerified         Boolean        @default(false)
  verificationToken     String?
  resetPasswordToken    String?        // Field for password reset token
  resetPasswordTokenExpiry DateTime?   // Field for token expiry
  createdAt             DateTime       @default(now())
  updatedAt             DateTime       @updatedAt
  loginHistory          LoginHistory[] @relation("UserToLoginHistory")
}

model LoginHistory {
  id        String   @id @default(uuid())
  userId    String
  action    String
  device    String?
  country   String?
  timestamp DateTime @default(now())
  user      User     @relation("UserToLoginHistory", fields: [userId], references: [id])
}
>>>>>>> 67d1c6d0ee34aa248b02a2f3693f7a2c3e685e15
